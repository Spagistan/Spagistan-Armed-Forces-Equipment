name: Build and Release

on:
  pull_request:
    branches:
      - "master"
  push:
    tags:
      - v*

jobs:
  build:
    name: Build
    runs-on: ubuntu-latest
    outputs:
      VERSION: ${{ github.ref_name }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0 # Needed to get all history for changelog

      - name: Setup HEMTT
        uses: arma-actions/hemtt@v1

      - name: Run HEMTT build (release mode)
        run: hemtt release

      - name: Rename build folder
        run: mv .hemttout/release .hemttout/@safe # Change "yourmodname" if necessary

      - name: Zip the mod
        run: |
          cd .hemttout
          zip -r @safe.zip @safe

  release:
    name: Release
    needs: build
    if: github.ref_type == 'tag'
    runs-on: ubuntu-latest
    steps:
      - name: Upload Mod Artifact
        uses: actions/upload-artifact@v4
        with:
          name: mod-artifact
          path: .hemttout/@safe.zip
          if-no-files-found: error

      - name: Download Artifact
        uses: actions/download-artifact@v4
        with:
          name: mod-artifact
          path: release-files

      - name: Unzip Mod
        run: |
          unzip release-files/@safe.zip -d .
          ls -la @safe

      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          files: release-files/@safe.zip
          name: ${{ needs.build.outputs.VERSION }}

      - name: Upload to Steam Workshop
        uses: arma-actions/workshop-upload@v1
        with:
          appId: "107410"
          itemId: "3615632492" # Dev Builds (change if needed)
          contentPath: "@safe"
          changelog: "Version ${{ needs.build.outputs.VERSION }}"
        env:
          STEAM_USERNAME: ${{ secrets.STEAM_USERNAME }}
          STEAM_PASSWORD: ${{ secrets.STEAM_PASSWORD }}
